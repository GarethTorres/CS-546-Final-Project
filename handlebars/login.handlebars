<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Login</title>
</head>
<body>
    <h1>Login</h1>
    {{#if errorMessage}}
        <p>{{errorMessage}}</p>
    {{/if}}
    <form method="POST" action="/login">
        <label for="emailAddressInput">Email address:</label>
        <input type="email" id="emailAddressInput" name="emailAddressInput" required>
        <br>
        <label for="passwordInput">Password:</label>
        <input type="password" id="passwordInput" name="passwordInput" required>
        <br>
        <button type="submit">Login</button>
    <div class="error-group alert alert-danger" hidden>
          <ul id = "results-login">
            {{#if errors}}
              <li>{{errors}}</li>
            {{/if}}
          </ul>
      </div>
    </div>
  </form>
  <form action = "/signup" class="signup-form">
    <div>Don't have an account? Sign-Up Here!</div>
    <input type = "submit" class = "btn btn-default" value = "Sign-Up"/>
  </form>
  <footer>&copy; 2023 My Showcase. All rights reserved.</footer>
  <script src="/public/js/login.js"></script>
