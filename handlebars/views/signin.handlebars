<body>
    <div class="container">
    <div class="row">
        <div class="col-sm-8">
            <br>
            <form id="signin-form" method="post" action="/users/signin">
                <div class="form-group">
                    <p class="welcome">Login</p>
                    <br/>
                    <p class="noPost" for="text1">Enter Your Username:</p>
                    <input type="email" class="form-control" id="signinFullName" name="Fullname" placeholder="fullname@xxx.com">
                    <br>
                    <p class="noPost" for="text1">Enter Your Password:</p>
                    <input type="password" class="form-control" id="signinPassword" name="password" placeholder="Password">
                    <br>
                    <div class="form-group">
                        <button type="submit" class="btn AllButton">Sign in</button>
                    </div>
                    {{#unless status}}
                        <p class="wrongMsg">{{message}}</p>
                    {{/unless}}
                </div>
            </form>
        </div>
    </div>
</div>
    <br/>
    <br/>
    <br/>
    <br/>
    <br/>
    <br/>
    <br/>
    <br/>
</body>

