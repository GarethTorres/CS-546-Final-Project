<div class="edit-showcase" id="editShowcaseDiv">
    <h1>
        Edit Showcase
    </h1>
    {{> editPartial showcaseInfo}}
    <div id="errorDiv">
    </div>
    <form id="editShowcaseForm">
        <label>New Showcase Text
            <br />
            <textarea id="editShowcaseText"  placeholder="Your text here">{{showcaseInfo.text}}</textarea>
        </label>
        <br />
        <label>New Blog (Optional)
            <br />
            <select id="editShowcaseBlog" class="form-control">
                <option value="None">None</option>
                {{#each allBlogs}}
                <option value="{{this.[1]}}">{{this.[0]}}</option>
                {{/each}}
            </select>
        </label>
        <br />
        <input type="submit" value="Edit Showcase" class="btn btn-primary mb-2" />
    </form>
    <form id="deleteShowcaseForm">
        <input type="submit" value="Delete Showcase" class="btn btn-primary mb-2" />
    </form>
    <div id="successDiv" hidden></div>
    <!-- Hidden showcaseID in order -->
    <p id="showcaseID" hidden>{{ showcaseInfo._id }}</p>
</div>
